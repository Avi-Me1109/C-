#include <iostream>
using namespace std;

int main()
{
	int integer1 = 1;
	
	cout << "\n";
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";	

	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}

	else
		cout << integer1 << " is not divisible by 3 and 5.\n";

	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}

	else
		cout << integer1 << " is not divisible by 3 and 5.\n";

	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}

	else
		cout << integer1 << " is not divisible by 3 and 5.\n";

	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
	
	integer1 = integer1 + 1;
	
	if (integer1 % 3 ==0)
	{	
		if (integer1 % 5 ==0)
			cout << integer1 << " is divisible by 3 and 5.\n";
		else
			cout << integer1 << " is not divisible by 3 and 5.\n";
	}
	
	else
		cout << integer1 << " is not divisible by 3 and 5.\n";
}
